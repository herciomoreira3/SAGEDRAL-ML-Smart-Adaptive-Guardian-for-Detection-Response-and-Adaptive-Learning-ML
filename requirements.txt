# ============================================================
# SAGEDRAL-ML — Requirements for Linux Installation
# Smart Adaptive Guardian for Detection, Response and
# Adaptive Learning — ML-based NIDPS System
#
# Python >= 3.9 required
# Usage: pip install -r requirements.txt
# ============================================================

# ── Core Network Capture ─────────────────────────────────────
scapy>=2.5.0               # Packet sniffing (promiscuous mode, AsyncSniffer)
dpkt>=1.9.8                # Packet parsing (PCAP replay / training data)

# ── Machine Learning ──────────────────────────────────────────
lightgbm>=4.0.0            # Gradient Boosting — Anomaly Detector & Attack Classifier
scikit-learn>=1.3.0        # IsolationForest, LabelEncoder, train_test_split
numpy>=1.24.0              # Numerical arrays & feature vectors
pandas>=2.0.0              # DataFrame handling for training pipeline
joblib>=1.3.0              # Model serialization (save/load .pkl)

# ── API Backend ───────────────────────────────────────────────
fastapi>=0.111.0           # Async REST API framework
uvicorn[standard]>=0.29.0  # ASGI server (supports WebSocket + HTTP/2)
websockets>=12.0           # WebSocket server support for uvicorn
httpx>=0.27.0              # Async HTTP client (used in tests + retrain trigger)

# ── Database (Async SQLite) ───────────────────────────────────
sqlalchemy[asyncio]>=2.0.0 # Async ORM with SQLite/PostgreSQL support
aiosqlite>=0.19.0          # Async SQLite driver for SQLAlchemy

# ── Configuration ─────────────────────────────────────────────
tomli>=2.0.1               # TOML parser for Python < 3.11
tomli-w>=1.0.0             # TOML writer (config export)

# ── CLI & HTTP ────────────────────────────────────────────────
click>=8.1.0               # Command line interface (sagedral-ml start/stop/train)
requests>=2.28.0           # HTTP client for CLI status/control calls

# ── System Monitoring ─────────────────────────────────────────
psutil>=5.9.0              # CPU/Memory/Network interface stats

# ============================================================
# Optional: Training & Visualization tools
# Install with: pip install -r requirements-train.txt
# ============================================================
